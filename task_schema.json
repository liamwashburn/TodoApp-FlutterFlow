{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Task",
  "description": "Defines task records created by users in the Firestore tasks collection",
  "type": "object",
  "properties": {
    "completed": {
      "description": "Indicates whether the task has been marked as finished",
      "type": "boolean"
    },
    "created": {
      "description": "Timestamp representing when the task was added",
      "type": "string",
      "format": "date-time"
    },
    "details": {
      "description": "Additional notes or description for the task",
      "type": "string"
    },
    "title": {
      "description": "Short name of the task displayed in the app",
      "type": "string"
    },
    "user": {
      "description": "Firestore reference path to the owner of the task",
      "type": "string"
    }
  },
  "required": [
    "completed",
    "created",
    "title",
    "user"
  ]
}
